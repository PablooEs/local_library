extends layout

block content

    p #[strong Do you really want to delete this Instance?]

    p ID: #{bookinstance._id}

    p #[strong Title:]
        #{bookinstance.book.title}
    p #[strong Imprint:] #{bookinstance.imprint}

    form(method='POST' action='')
        div.form-group
        input#bookinstance.id.form-control(type='hidden',name='bookinstanceid', required='true', value=bookinstance._id )

        button.btn.btn-primary(type='submit') Delete